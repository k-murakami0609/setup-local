font-family = "UDEV Gothic"
font-size = 20

# 透過設定
background-opacity = 0.8
background-blur-radius = 20

# 前後のタブへ移動 (Control + Option + 方向キー)
keybind = command+opt+left=previous_tab
keybind = command+opt+right=next_tab
